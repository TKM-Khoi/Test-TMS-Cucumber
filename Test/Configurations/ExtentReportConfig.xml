<?xml version="1.0" encoding="utf-8"?>
<configuration>

	<!-- report theme -->
	<!-- Standard, Dark -->
	<theme>Standard</theme>

	<!-- document encoding -->
	<!-- defaults to UTF-8 -->
	<encoding>UTF-8</encoding>

	<!-- protocol for script and stylesheets -->
	<!-- defaults to https -->
	<!-- HTTP, HTTPS -->
	<protocol>HTTPS</protocol>

	<!-- offline report -->
	<timelineEnabled>true</timelineEnabled>

	<!-- offline report -->
	<enableOfflineMode>false</enableOfflineMode>

	<!-- use thumbnails for base64 images -->
	<!-- this may slowdown viewing tests -->
	<thumbnailForBase64>false</thumbnailForBase64>

	<!-- title of the document -->
	<documentTitle>Extent Framework</documentTitle>

	<!-- report name - displayed at top-nav -->
	<reportName>Build 1</reportName>

  <dateFormat>dd/MMM/yyyy</dateFormat>
  <timeFormat>HH:mm:ss</timeFormat>
	<!-- timestamp format -->

	<!-- custom javascript -->
	<scripts>
		<![CDATA[
      $(document).ready(function() {
        //alert('Welcome to summoner rift.\nBe ready to carry my 0/10 Yasuo and face verbal abuse.');
      });
		]]>
	</scripts>

	<!-- custom styles -->
	<styles>
	<![CDATA[
      
	]]>
	</styles>

</configuration>